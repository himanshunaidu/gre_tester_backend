//Generate a select query that takes table_name, columns, where, orderby, groupby, having
const selectQuery = () => {

}

//Generate an insert query that takes table_name, columns, values
const insertQuery = () => {
    
}